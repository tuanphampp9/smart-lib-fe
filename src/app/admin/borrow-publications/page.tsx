import * as React from 'react';

export interface IBorrowPublicationProps {
}

export default function BorrowPublication (props: IBorrowPublicationProps) {
  return (
    <div>
      hello borrow publication
    </div>
  );
}
